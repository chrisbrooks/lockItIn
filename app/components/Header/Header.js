import styles from './Header.less';

import React from 'react';

const Header = () => {
    return (
        <h1 className={styles.h1}>Would you like to make a payment?</h1>
    );
};

export default Header;
